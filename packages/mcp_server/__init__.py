"""MCP (Model Context Protocol) server for the agent-queue system.

Exposes agent-queue resources, tools, and prompts to MCP-compatible clients
via stdio transport. Uses the official MCP Python SDK (FastMCP).
"""
